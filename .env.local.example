# Example only — never commit real credentials to a public repo.
# Copy to .env.local and replace placeholders if you add a backend later.

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
